# Ordered CRM rules. First match wins.
# Format:
# rule_name=<name>|condition=<python/pandas expression>|segment_id=<id>|segment_name=<label>|campaign=<campaign>|action=<action>|summary=<summary>
#
# Decision priority mirrors the CRM documentation:
# 1. physiological_churn
# 2. high_churn
# 3. high_engagement
# 4. points_close
#
# Variables exposed to the rule expressions:
# - physiological_churn: 1 when the user is already in the lifecycle transition window
# - high_churn: 1 when churn_score >= 0.50
# - high_engagement: 1 when re_engage30d >= 0.50
# - points_close: 1 when the user can already redeem or is within 600 points of the next higher reward threshold

# S1: lifecycle-transition users who should be converted into referral value rather than win-back.
rule_name=lifecycle_transition_churn|condition=physiological_churn == 1|segment_id=S1|segment_name=Lifecycle-transition churn|segment_description=Users in the lifecycle transition window who should receive a referral-led exit campaign instead of a retention push.|campaign=Referral campaign|action=Use a loyalty exit message that rewards the user for referring a pregnant friend or new parent instead of trying to win them back.|summary=Lifecycle transition overrides every other signal, so the campaign should convert likely exit into referral value.

# S2: all non-lifecycle-transition high-churn users consolidated into one rescue audience.
rule_name=high_churn_rescue|condition=high_churn == 1|segment_id=S2|segment_name=High-churn rescue|segment_description=High-churn users who still fit the app lifecycle and should receive one strong rescue campaign rather than sparse micro-segments.|campaign=Double points rescue|action=Use a strong limited-window rescue incentive for every non-lifecycle-transition high-churn user, regardless of engagement or points distance, because the smaller high-risk subsegments are too sparse to campaign separately.|summary=All non-lifecycle-transition high-churn users are consolidated into one rescue segment to avoid over-fragmenting tiny audiences.

# S3: low-churn users with an immediate reward hook.
rule_name=low_churn_reward_near|condition=high_churn == 0 & points_close == 1|segment_id=S3|segment_name=Low-churn, reward-near|segment_description=Low-churn users who are already near a reward threshold and only need a celebratory prize reminder.|campaign=Prize reminder|action=Keep the tone celebratory and remind the user how close they are to the next reward without adding urgency or rescue language.|summary=Low churn removes retention pressure, so the message can simply celebrate progress toward a prize.

# S4: low-churn, high-engagement users who are far from a reward and need acceleration, not rescue.
rule_name=low_churn_high_engagement_reward_far|condition=high_churn == 0 & points_close == 0 & high_engagement == 1|segment_id=S4|segment_name=Low-churn, high-engagement, reward-far|segment_description=Low-churn, highly engaged users who are far from a reward and should receive a warm acceleration offer to deepen engagement.|campaign={preferred_incentive_title}|action=Use a warm acceleration campaign that helps the user earn faster while they are already engaged with the app.|summary=The user is low-churn and already showing engagement, so the goal is to deepen engagement and accelerate reward progress rather than rescue retention.

# S5: low-churn, low-engagement users who need a benefits-led educational reminder.
rule_name=low_churn_low_engagement_reward_far|condition=True|segment_id=S5|segment_name=Low-churn, low-engagement, reward-far|segment_description=Users with low churn risk and low current engagement, whether newly onboarded or inactive for a while, who should receive an educational reminder about the app's benefits instead of a rescue campaign.|campaign=Educational campaign|action=Use an educational reminder that highlights the app's main benefits and one simple next step to get value from it again, without using urgency or rescue language.{education_clause}|summary=The user is low-churn but not very engaged, so the goal is to rebuild relevance by reminding them why the app is useful and how to benefit from it.
